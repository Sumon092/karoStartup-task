import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center'>
            <h4>Loading...</h4>
        </div>
    );
};

export default Loading;